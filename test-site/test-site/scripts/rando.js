// This is a comment.

/* 
So is this.
This one can be mulitiple lines long.
*/

function setRandomNum() {
    const rand = Math.random();
    const elem = document.getElementById("rand_num");
    elem.innerHTML = rand;
}

